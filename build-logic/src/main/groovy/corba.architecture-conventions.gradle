// Use this plugin for projects that run repository-wide architecture checks.
plugins {
    // Architecture tests are Java tests, so they use the same Java library defaults.
    id 'corba.java-library-conventions'
}

// Architecture tests are implemented in modules/corba-architecture-tests.
// This plugin exists so future modules can opt into repository-wide architecture checks.
