/**
 * idlj-like command-line validation entry points for the approved IDL compiler slice.
 *
 * <p>This package validates IDL source through the parser and semantic model. It does not generate
 * Java source, codecs, repository IDs, native-image metadata, or ORB runtime artifacts.
 */
package io.github.mundanej.mjo.idlj;
