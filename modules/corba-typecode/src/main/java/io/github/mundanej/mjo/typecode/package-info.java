/**
 * Static IDL type descriptors, local TypeCode metadata, and compile-only codec surfaces.
 *
 * <p>This package provides generated-descriptor runtime values and immutable local TypeCode
 * metadata for the supported Any slice. Full CORBA wire TypeCode marshaling, DynamicAny, Interface
 * Repository integration, and dynamic invocation remain later roadmap work.
 */
package io.github.mundanej.mjo.typecode;
