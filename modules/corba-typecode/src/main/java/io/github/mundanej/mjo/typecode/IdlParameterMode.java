package io.github.mundanej.mjo.typecode;

/** IDL operation parameter passing modes. */
public enum IdlParameterMode {
  /** Input-only parameter. */
  IN,
  /** Output-only parameter. */
  OUT,
  /** Input/output parameter. */
  INOUT
}
