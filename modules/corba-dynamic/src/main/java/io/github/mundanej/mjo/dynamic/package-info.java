/**
 * Local descriptor-backed DynamicAny, DII, and DSI support.
 *
 * <p>This package adapts generated static descriptors, local Any values, and local ORB invocation.
 * It intentionally does not implement OMG compatibility APIs, wire requests, reflection-driven
 * invocation, or peer interoperability.
 */
package io.github.mundanej.mjo.dynamic;
