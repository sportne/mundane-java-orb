/**
 * Semantic symbol resolution and constant evaluation for the approved OMG IDL parser slice.
 *
 * <p>This package consumes immutable AST values and emits deterministic semantic models with stable
 * diagnostics. It does not perform repository ID generation, Java mapping, source generation,
 * protocol encoding, ORB runtime behavior, reflection, or dynamic scanning.
 */
package io.github.mundanej.mjo.idl.semantics;
