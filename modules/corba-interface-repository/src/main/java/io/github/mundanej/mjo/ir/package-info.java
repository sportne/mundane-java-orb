/**
 * Local static Interface Repository metadata over generated IDL descriptors.
 *
 * <p>This package intentionally uses explicit descriptor lists and does not scan the classpath or
 * expose networked CORBA Interface Repository objects.
 */
package io.github.mundanej.mjo.ir;
