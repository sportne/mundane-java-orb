/**
 * Local Notification Service channel lifecycle and Event Service compatibility boundary.
 *
 * <p>This package intentionally starts with local, explicit runtime objects. Structured events,
 * filtering, QoS/admin policy, delivery, IIOP exposure, Native Image smoke, and peer interop are
 * staged by later roadmap tasks.
 */
package io.github.mundanej.mjo.notification;
