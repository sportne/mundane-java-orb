/**
 * RMI-IIOP and Java-to-IDL planning surfaces.
 *
 * <p>This package currently exposes explicit declaration models, deterministic eligibility
 * diagnostics, an in-memory Java-to-IDL model, and explicit metadata-based RMI repository ID
 * planning, plus deterministic generated IDL fixtures for the parser-supported subset. It does not
 * load application classes, inspect classpaths, compute Java serialization hashes, generate Java
 * bindings, invoke an ORB, or marshal values on the wire.
 */
package io.github.mundanej.mjo.rmi.iiop;
