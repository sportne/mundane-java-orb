/**
 * RMI-IIOP and Java-to-IDL planning surfaces.
 *
 * <p>This package currently exposes explicit declaration models, deterministic eligibility
 * diagnostics, and an in-memory Java-to-IDL model for G7-020. It does not load application classes,
 * inspect classpaths, generate IDL or Java bindings, invoke an ORB, or marshal values on the wire.
 */
package io.github.mundanej.mjo.rmi.iiop;
