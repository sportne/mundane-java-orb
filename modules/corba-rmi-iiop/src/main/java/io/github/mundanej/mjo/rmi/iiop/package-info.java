/**
 * RMI-IIOP and Java-to-IDL planning surfaces.
 *
 * <p>This package currently exposes only explicit declaration models and deterministic eligibility
 * diagnostics for G7-010. It does not load application classes, inspect classpaths, generate IDL or
 * Java bindings, invoke an ORB, or marshal values on the wire.
 */
package io.github.mundanej.mjo.rmi.iiop;
