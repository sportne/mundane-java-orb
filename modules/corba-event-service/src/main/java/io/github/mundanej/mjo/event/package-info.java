/**
 * Local Event Service channel lifecycle model.
 *
 * <p>This package intentionally starts with local, explicit runtime objects. Push/pull delivery,
 * IIOP exposure, Native Image smoke, and peer interop are staged by later roadmap tasks.
 */
package io.github.mundanej.mjo.event;
