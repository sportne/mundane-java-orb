package io.github.mundanej.mjo.idl.ast;

/** Interface modifier category for IDL interface declarations and forwards. */
public enum IdlInterfaceKind {
  /** Ordinary IDL interface. */
  NORMAL,
  /** Abstract IDL interface. */
  ABSTRACT,
  /** Local IDL interface. */
  LOCAL
}
