package io.github.mundanej.mjo.idl.ast;

/** Visibility category for valuetype state members. */
public enum IdlValueVisibility {
  /** Public valuetype state member. */
  PUBLIC,
  /** Private valuetype state member. */
  PRIVATE
}
