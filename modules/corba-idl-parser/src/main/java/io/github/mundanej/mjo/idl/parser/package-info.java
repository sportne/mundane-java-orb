/**
 * Minimal recursive-descent parser for the approved OMG IDL grammar slice.
 *
 * <p>The parser builds syntax-only AST nodes. It intentionally does not perform semantic symbol
 * resolution, type checking, repository ID construction, Java mapping, or code generation.
 */
package io.github.mundanej.mjo.idl.parser;
