/**
 * OMG IDL 4.2 lexical scanning.
 *
 * <p>This package only turns source text into tokens and lexical diagnostics. Parser grammar,
 * include resolution, preprocessing semantics, AST construction, semantic analysis, code
 * generation, and runtime behavior are implemented by later roadmap tasks.
 */
package io.github.mundanej.mjo.idl.lexer;
