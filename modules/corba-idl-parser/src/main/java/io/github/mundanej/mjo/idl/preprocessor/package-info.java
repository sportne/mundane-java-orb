/**
 * Minimal C-style IDL preprocessing infrastructure.
 *
 * <p>This package expands includes, line continuations, selected macro forms, and selected
 * conditional directives into a deterministic token stream. It does not implement parser grammar,
 * AST construction, semantic analysis, code generation, ORB/runtime behavior, or protocol behavior.
 */
package io.github.mundanej.mjo.idl.preprocessor;
